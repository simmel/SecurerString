apply plugin: 'java'
defaultTasks 'build'

repositories {
  mavenCentral()
}

dependencies {
  testCompile 'junit:junit:4.+'
}

jar {
  manifest {
    attributes 'Main-Class': "se.soy.${project.name.toLowerCase()}.${project.name}"
  }
}
